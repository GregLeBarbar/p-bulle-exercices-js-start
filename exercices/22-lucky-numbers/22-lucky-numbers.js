// @ts-check

/**
 * Additionne deux tableaux de chiffres interprétés comme des nombres concaténés.
 * Ex. [2,4] => 24 ; [1,5,7] => 157 ; 24 + 157 => 181
 * @param {number[]} array1
 * @param {number[]} array2
 * @returns {number}
 */
export function twoSum(array1, array2) {
  throw new Error('À implémenter');
}

/**
 * Retourne true si le nombre est palindromique.
 * @param {number} value
 * @returns {boolean}
 */
export function luckyNumber(value) {
  throw new Error('À implémenter');
}

/**
 * Retourne le message d'erreur pour une saisie donnée.
 * - '', null, undefined        → "Required field"
 * - non num./0 (après trim)    → "Must be a number besides 0"
 * - sinon                       → ""
 * @param {string|null|undefined} input
 * @returns {string}
 */
export function errorMessage(input) {
  throw new Error('À implémenter');
}

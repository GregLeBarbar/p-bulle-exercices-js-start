/**
 * 📄 Description :
 * Écris une fonction max(a, b) qui retourne le plus grand des deux nombres.
 *
 * Exemple :
 * ```js
 * max(2, 3) // 3
 * max(5, 1) // 5
 * max(4, 4) // 4
 * ```
 **/
function max(a, b) {
  throw new Error('À implémenter');
}

export { max };

/**
 * 📄 Description :
 * Crée une fonction `bothTrue(a, b)` qui retourne `true`
 * uniquement si `a` et `b` sont tous les deux vrais.
 *
 * Exemple :
 * ```js
 * bothTrue(true, true) // true
 * bothTrue(true, false) // false
 * ```
 **/
function bothTrue(a, b) {
  throw new Error('À implémenter');
}

export { bothTrue };

/**
 * 📄 Description :
 * Crée une fonction `repeatString(str, times)` qui retourne une nouvelle chaîne
 * contenant `str` répétée `times` fois.
 *
 * Exemple :
 * ```js
 * repeatString("yo", 3) // "yoyoyo"
 * repeatString("a", 0) // ""
 * ```
 **/
function repeatString(str, times) {
  throw new Error('À implémenter');
}

export { repeatString };
